defaults:
  - _self_

server:
  host: 0.0.0.0
  port: 5000
  debug: false

frozenlake:
  max_workers: 48
svg:
  max_workers: 48
  model_size: "small"
navigation:
  max_workers: 48
  devices: [0]
primitive_skill:
  max_workers: 48

