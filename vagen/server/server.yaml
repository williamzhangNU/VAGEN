defaults:
  - _self_

server:
  host: 127.0.0.1
  port: 5000
  debug: false

frozenlake:
  max_workers: 48
svg:
  max_workers: 48
  model_size: "small"
navigation:
  max_workers: 48
  devices: [0,1,2,3]
primitive_skill:
  max_workers: 48

