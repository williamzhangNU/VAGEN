# config.yaml
name: demo_env
render_mode: text  # options: vision or text

seed-range: 0-9
exp_type: active 
# perspective: ego
max_exp_steps: 20
calculate_information_gain: true
data_dir: "/home/zihanhuang/VAGEN/data-test"
eval_task_counts:
  dir: 3
  # fwd_fov: 3
  # bwd_nav: 3
  # rot: 3
  # rot_dual: 3
  # pov: 3
  # bwd_pov: 3
  # e2a: 3
  # fwd_loc: 3
  # bwd_loc: 3
  # false_belief: 3

prompt_config:
  topdown: false
  oblique: false
  type: shorter
  enable_think: true


kwargs:
  model_config:
    model_name: human_player
  all_override: true
  output_dir: "results"
