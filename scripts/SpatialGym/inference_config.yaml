# Server configuration
server_url: "http://localhost:5000"
server_timeout: 600
server_max_workers: 48

# Inference parameters
batch_size: 32
max_steps: 20
split: "test"
debug: false

# Output configuration
output_dir: "results/pass_rot"

# WandB configuration
use_wandb: false
wandb_project: "vagen-debug"
val_generations_to_log_to_wandb: 10 

# Display settings
show_progress: true